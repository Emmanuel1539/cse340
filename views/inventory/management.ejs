<% if (title) { %>
    <h1><%= title %></h1>
<% } else { %>
    <%- res.redirect('/') %>
<% } %>

<%- messages() %>

<h2>Welcome <%= res.locals.accountData.firstName %></h2>

<!-- Update Account Information link for all users -->
<p><a href="/account/update/<%= res.locals.accountData._id %>">Update Account Information</a></p>

<!-- Conditional rendering for Manage Inventory section -->
<% if (res.locals.accountData.userType === 'Employee' || res.locals.accountData.userType === 'Admin') { %>
    <h3>Inventory Management</h3>
    <p><a href="/inventory">Access Inventory Management</a></p>
<% } %>

<!-- Rendered for both logged-in clients and admin/employee types -->
<h2>Manage Inventory</h2>
<p>Choose a classification to see those inventory items.</p>

<%- classificationSelect %>
<table id="inventoryDisplay"></table>

<noscript>JavaScript must be enabled to use this page</noscript>
<script src="../../js/inventory.js"></script>
